apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-load-planning-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-load-planning-service
  template:
    metadata:
      labels:
        app: aircargo-load-planning-service
    spec:
      containers:
        - name: aircargo-load-planning-service
          image: aircargo/aircargo-load-planning-service:latest
          ports:
            - containerPort: 9098
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9098
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9098
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-load-planning-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-load-planning-service
  ports:
    - port: 9098
      targetPort: 9098
