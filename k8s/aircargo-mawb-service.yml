apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-mawb-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-mawb-service
  template:
    metadata:
      labels:
        app: aircargo-mawb-service
    spec:
      containers:
        - name: aircargo-mawb-service
          image: aircargo/aircargo-mawb-service:latest
          ports:
            - containerPort: 9095
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9095
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9095
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-mawb-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-mawb-service
  ports:
    - port: 9095
      targetPort: 9095
