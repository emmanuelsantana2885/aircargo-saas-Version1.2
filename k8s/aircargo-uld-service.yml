apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-uld-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-uld-service
  template:
    metadata:
      labels:
        app: aircargo-uld-service
    spec:
      containers:
        - name: aircargo-uld-service
          image: aircargo/aircargo-uld-service:latest
          ports:
            - containerPort: 9097
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9097
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9097
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-uld-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-uld-service
  ports:
    - port: 9097
      targetPort: 9097
