apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-flight-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-flight-service
  template:
    metadata:
      labels:
        app: aircargo-flight-service
    spec:
      containers:
        - name: aircargo-flight-service
          image: aircargo/aircargo-flight-service:latest
          ports:
            - containerPort: 9093
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9093
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9093
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-flight-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-flight-service
  ports:
    - port: 9093
      targetPort: 9093
