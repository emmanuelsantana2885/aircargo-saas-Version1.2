apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-warehouse-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-warehouse-service
  template:
    metadata:
      labels:
        app: aircargo-warehouse-service
    spec:
      containers:
        - name: aircargo-warehouse-service
          image: aircargo/aircargo-warehouse-service:latest
          ports:
            - containerPort: 9096
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9096
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9096
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-warehouse-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-warehouse-service
  ports:
    - port: 9096
      targetPort: 9096
