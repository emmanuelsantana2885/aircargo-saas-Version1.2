apiVersion: apps/v1
kind: Deployment
metadata:
  name: aircargo-auth-service
  namespace: aircargo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aircargo-auth-service
  template:
    metadata:
      labels:
        app: aircargo-auth-service
    spec:
      containers:
        - name: aircargo-auth-service
          image: aircargo/aircargo-auth-service:latest
          ports:
            - containerPort: 9092
          envFrom:
            - configMapRef:
                name: aircargo-config
            - secretRef:
                name: aircargo-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9092
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9092
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: aircargo-auth-service
  namespace: aircargo
spec:
  selector:
    app: aircargo-auth-service
  ports:
    - port: 9092
      targetPort: 9092
